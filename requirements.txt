Flask==2.1.3
Werkzeug==2.1.2

# PDF processing
pdf2docx==0.5.8
PyPDF2==3.0.1
PyMuPDF==1.22.5
pdf2image==1.16.3
pikepdf==8.13.0  # Thêm để xử lý PDF tốt hơn

# Office documents
python-docx==0.8.11
python-pptx==0.6.21

# Image processing
Pillow==10.1.0
img2pdf==0.4.4  # Thêm để chuyển ảnh sang PDF tốt hơn

# Utilities
requests==2.28.2  # Thêm nếu cần gọi API bên ngoài
python-magic==0.4.27  # Kiểm tra loại file chính xác hơn